# Example: dataset project

Observed files: CSV data, schema, collection script, and an internal note containing source URLs.

The Skill should classify the project as a dataset, prepare a data card, preserve provenance, identify sensitive fields, record license/allowed use, and provide a validation command. It should stop for a decision if source permission is unclear. A software-style feature list is not sufficient evidence.
