# Example: software project

Observed files: `pyproject.toml`, a CLI entry point, unit tests, and no README.

The Skill should classify the project as software, run the native tests, create a concise install → first-success path, document the verified Python version, add a license decision, and package the repository. It should not add a data card, curriculum, design gallery, or fixed launch calendar.
